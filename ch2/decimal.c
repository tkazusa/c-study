#include <stdio.h>

int main(void){

  int a = 15, b = 0x11;

  printf("%d in decimal is same as %x in hexa decimal\n", a, a);
  printf("%x in hexa-decimal is same as %d in deximal\n", b, b);

  return 0;

}
